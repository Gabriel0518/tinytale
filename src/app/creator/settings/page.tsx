import CreatorPlaceholderPage from "../_components/CreatorPlaceholderPage";

export default function CreatorSettingsPage() {
  return (
    <CreatorPlaceholderPage
      title="Settings"
      description="Creator account settings hub for profile, payout setup, security, and notification preferences."
      route="/creator/settings"
    />
  );
}
