import CreatorPlaceholderPage from "../_components/CreatorPlaceholderPage";

export default function CreatorTicketsPage() {
  return (
    <CreatorPlaceholderPage
      title="Support Tickets"
      description="Ticket center for creator support requests, status tracking, and SLA updates."
      route="/creator/tickets"
    />
  );
}
